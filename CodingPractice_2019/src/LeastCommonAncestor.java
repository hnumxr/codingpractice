import LeetCodeTrees.TreeNode;

public class LeastCommonAncestor {

    /*
        Steps:
        1. Check if B exists in the tree; if doesn't, return -1;
        2. Check if C exists in the tree; if doesn't, return -1;
        3. Find ancestor list for B as ancestorListB;
        4. Find C's ancestor and check if it exists in ancestorListB, if it exists, return it; otherwise find its ancestor continuously.
    */

    public int FindLeastCommonAncestor(TreeNode A, int B, int C) {
        int ancestor = -1;

        return  ancestor;
    }
}
